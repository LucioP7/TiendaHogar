<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Movil.ViewModels"
             x:Class="Movil.Views.ProductosEnOfertaView"
             Title="Productos en oferta"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.BindingContext>
        <viewModels:ProductosEnOfertaViewModel />
    </ContentPage.BindingContext>

    <VerticalStackLayout Spacing="8" Padding="16">
        <Frame Padding="8" CornerRadius="12">
            <Label Text="Lista de productos en oferta"
                   HorizontalOptions="Center"
                   FontSize="18"
                   FontAttributes="Bold" />
        </Frame>

        <SearchBar Placeholder="Buscar producto en oferta..."
                   Text="{Binding FilterProducts}"
                   SearchCommand="{Binding FiltrarProductosCommand}" />

        <ActivityIndicator IsRunning="{Binding ActivityStart}" IsVisible="{Binding ActivityStart}" />

        <CollectionView ItemsSource="{Binding Productos}">
            <CollectionView.EmptyView>
                <Label Text="Cargando ofertas disponibles..."
                       HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12" Margin="0,6" HasShadow="True">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="{Binding Nombre}" VerticalOptions="Center" />
                            <Label Grid.Column="1"
                                   Text="{Binding Precio, StringFormat='${0:F2}'}"
                                   VerticalOptions="Center"
                                   FontAttributes="Bold" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>