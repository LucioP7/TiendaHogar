<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Movil.TiendaShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Movil"
    xmlns:views="clr-namespace:Movil.Views"
    xmlns:viewsmodels="clr-namespace:Movil.ViewModels"
    xmlns:contentviews="clr-namespace:Movil.ContentViews"
    FlyoutBehavior="Disabled"
    Title="Movil"
    FlyoutBackgroundImage="fondo.png"
    FlyoutBackgroundImageAspect="AspectFill"
    FlyoutHeaderBehavior="CollapseOnScroll">

    <Shell.BindingContext>
        <viewsmodels:TiendaShellViewModel />
    </Shell.BindingContext>

    <Shell.FlyoutHeader>
        <contentviews:FlyoutHeader />
    </Shell.FlyoutHeader>

    <Shell.FlyoutFooter>
        <contentviews:FlyoutFooter />
    </Shell.FlyoutFooter>

    <FlyoutItem Route="nuestra_app" 
            FlyoutDisplayOptions="AsMultipleItems" x:Name="FlyoutItemsPrincipal" >
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Icon="casa1.png"
            Route="MainPage" />
        <ShellContent
            Title="Ofertas"
            ContentTemplate="{DataTemplate views:ProductosEnOfertaView}"
            Icon="oferta.png"
            Route="Ofertas" /> 
        <Tab Title="Productos"
             Route="productos"
             Icon="electrodomesticos.png">
            <ShellContent
                Title="Lista"
                Icon="rango.png"
                ContentTemplate="{DataTemplate views:ProductosView}"
                Route="ListaProductos" />
            <ShellContent
                Title="Agregar producto"
                Icon="electronica.png"
                ContentTemplate="{DataTemplate views:AddEditProductoView}"
                Route="AgregarEditarProducto" />
        </Tab>
    </FlyoutItem>
    <MenuItem Text="Cerrar sesión"
          Command="{Binding LogoutCommand}"
          IconImageSource="cerrar.png"/>
    <ShellContent
            Title="Login"
            ContentTemplate="{DataTemplate views:IniciarSesionView}"
            Route="Login" 
            IsVisible="{Binding IsUserLogout}"
        />


</Shell>
